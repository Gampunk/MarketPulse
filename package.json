{
  "name": "marketpulse",
  "version": "0.1.0",
  "description": "Real-time crypto and forex market dashboard",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "@vercel/node": "^5.8.2",
    "typescript": "^6.0.3"
  }
}
